{
  "version": 2,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["index.html", "assets/**", "admin/**", "server/**"]
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/api/index" }]
}
